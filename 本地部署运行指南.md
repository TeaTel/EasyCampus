# EasyCampus（易校）本地部署运行指南

## 一、环境要求

| 依赖 | 版本要求 | 说明 |
|------|---------|------|
| JDK | 17 或更高 | 后端运行环境 |
| Maven | 3.6+ | 后端构建工具（项目自带 mvnw，可不单独安装） |
| Node.js | 18 或更高 | 前端运行环境 |
| npm | 9 或更高 | 前端包管理器 |
| MySQL | 8.0 | 数据库 |

---

## 二、项目结构

```
Campus-Community-AppV1.2-2.0-main/
├── backend/                    # Spring Boot 后端
│   ├── src/main/java/         # Java 源代码
│   ├── src/main/resources/    # 配置文件 + SQL 脚本
│   ├── pom.xml                # Maven 依赖配置
│   └── mvnw                   # Maven Wrapper（Linux/Mac）
├── frontend/                   # Vue 3 前端
│   ├── src/                   # 前端源代码
│   ├── package.json           # npm 依赖配置
│   └── vite.config.js         # Vite 构建配置
└── 报告/                       # 项目文档与报告
```

---

## 三、数据库配置

### 3.1 创建数据库

登录 MySQL 后执行：

```sql
CREATE DATABASE IF NOT EXISTS campus_market_dev
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
```

### 3.2 导入表结构

在项目根目录下执行（替换为你的 MySQL 用户名和密码）：

```bash
mysql -u root -p campus_market_dev < backend/src/main/resources/schema-mysql.sql
```

### 3.3 导入初始数据（可选）

```bash
mysql -u root -p campus_market_dev < backend/src/main/resources/data-mysql.sql
```

> **注意**：如果数据库密码不是 `123456`，请修改 `backend/src/main/resources/application-dev.yml` 中的 `spring.datasource.password` 字段。

---

## 四、后端启动

### 4.1 配置文件说明

开发环境配置文件：`backend/src/main/resources/application-dev.yml`

关键配置项：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `spring.datasource.url` | `jdbc:mysql://localhost:3306/campus_market_dev` | 数据库连接 |
| `spring.datasource.username` | `root` | 数据库用户名 |
| `spring.datasource.password` | `123456` | 数据库密码 |
| `server.port` | `8080` | 后端服务端口 |
| `jwt.secret` | 代码内置默认值 | JWT 签名密钥（位于 JwtUtil.java） |

### 4.2 启动命令

**macOS / Linux：**

```bash
cd backend
./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
```

**Windows：**

```cmd
cd backend
mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=dev
```

### 4.3 验证启动成功

浏览器访问：`http://localhost:8080/actuator/health`

返回 `{"status":"UP"}` 表示启动成功。

Swagger API 文档：`http://localhost:8080/swagger-ui.html`

---

## 五、前端启动

### 5.1 安装依赖

```bash
cd frontend
npm install
```

### 5.2 配置文件说明

开发环境配置文件：`frontend/.env.development`

```
VITE_API_BASE_URL=http://localhost:8080
VITE_APP_TITLE=校园二手交易平台 - 开发环境
```

### 5.3 启动命令

```bash
npm run dev
```

启动后访问：`http://localhost:3000`

---

## 六、技术栈概览

### 后端
- **框架**：Spring Boot 3.3.5
- **ORM**：MyBatis 3.0.3
- **安全**：Spring Security + JWT（jjwt 0.11.5）
- **数据库**：MySQL 8.0
- **API 文档**：Knife4j（Swagger）
- **工具库**：Lombok、Jackson、Thumbnailator

### 前端
- **框架**：Vue 3（Composition API + `<script setup>`）
- **构建工具**：Vite 5
- **路由**：Vue Router 4
- **状态管理**：Pinia
- **HTTP 客户端**：Axios
- **实时通信**：WebSocket（聊天功能）

---

## 七、常见问题

### Q1：后端启动报错 "Access denied for user"
> 检查 `application-dev.yml` 中的数据库用户名和密码是否正确。

### Q2：前端启动后页面空白
> 确认后端已启动，且 `VITE_API_BASE_URL` 指向正确的后端地址。

### Q3：端口被占用
> 后端默认 8080，前端默认 3000。可在配置文件中修改端口。

### Q4：Maven 下载依赖慢
> 可在 `~/.m2/settings.xml` 中配置阿里云镜像加速。

---

## 八、账户信息

- 开发环境数据库用户：`root` / `123456`
- JWT 默认密钥：代码内置（`JwtUtil.java` 第 18 行 `defaultSecretKeyForJwtTokenGenerationInCampusSecondHandPlatform`）
- 前端 API 地址：`http://localhost:8080`
